# Configurações de Ambiente - AgroTech Sprint 1
# Copie este arquivo para .env e configure as variáveis

# Ambiente de execução
FLASK_ENV=production
FLASK_CONFIG=production
FLASK_DEBUG=false

# Segurança
SECRET_KEY=agrotech-sprint1-production-key-2025-minimo-32-caracteres

# Banco de Dados (Railway PostgreSQL)
# Para desenvolvimento local (SQLite):
# DATABASE_URL=sqlite:///agente_agricola.db
# Para PostgreSQL Railway (Produção):
DATABASE_URL=postgresql://postgres:password@postgres.railway.internal:5432/railway

# Servidor
PORT=5000
HOST=0.0.0.0

# APIs Externas (opcional)
OPENAI_API_KEY=sua-chave-openai-aqui
WEATHER_API_KEY=sua-chave-weather-aqui

# Configurações de Sessão
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Lax

# Configurações de Cache (opcional)
REDIS_URL=redis://default:password@redis.railway.internal:6379

# Configurações de Email (opcional)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=1
MAIL_USERNAME=seu-email@gmail.com
MAIL_PASSWORD=sua-senha-de-app